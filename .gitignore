# segredos — a chave NUNCA entra no repo (leitura só por env OPENROUTER_API_KEY)
.env
.env.*
*.key

# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
dist/
build/

# artefatos de execução local (checkpoints/ledger são do USUÁRIO, não do repo)
_checkpoints/
.squad_spend/
_spend_ledger.jsonl
*.local.jsonl

# MEDIÇÕES PRIVADAS (decisão 1: modulo publico = maquinaria + personas; edge medido fica FORA)
# ground-truth e relatorios de eval sao a vantagem competitiva — nunca no repo publico.
# rosters medidos entram por override privado AI_SQUAD_ROSTER_<ROLE>, nao commitados.
ground_truth/
_candidate_evals/
rosters/

# OS/editor
.DS_Store
Thumbs.db
.vscode/
.idea/
